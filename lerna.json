{
  "npmClient": "yarn",
  "packages": ["packages/rpcs/*", "packages/*"],
  "version": "independent",
  "command": {
    "publish": {
      "ignoreChanges": ["ignored-file", "*.md"],
      "message": "chore(release): publish %s"
    }
  },
  "useWorkspaces": true
}
