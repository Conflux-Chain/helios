module.exports = function (/* context, options */) {
  return {
    name: '@cfxjs/docusaurus-plugin-webpack',
    configureWebpack(/* config, isServer, utils */) {
      // debugger
      // return {...config}
      return {}
    },
  }
}
