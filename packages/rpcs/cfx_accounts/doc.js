export default {
  metadata: {
    provider: true,
  },
  en: `Get current connected accounts from wallet.

Return an array with one address when connected and an empty array when not connected or wallet is locked.`,
}
