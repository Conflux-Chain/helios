import {describe, test} from 'vitest'
// eslint-disable-next-line no-unused-vars
import waitForExpect from 'wait-for-expect'

describe('token list schema', () => {
  test.todo('token list schema')
})
